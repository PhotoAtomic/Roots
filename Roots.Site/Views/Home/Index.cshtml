@using Roots.SupportedFileTypes
@{
    ViewBag.Title = "Home Page";
}
@section scripts{


    @Scripts.Render("~/Generated/Index","~/bundles/angularjs", "~/signalr/hubs", "~/bundles/treeview", "~/scripts/controllers/home.js")
    @Scripts.Render("/Scripts/marvin4js/gui/gui.nocache.js")
    @Styles.Render("/bundles/treeviewcs")
    
}
@*<script language="javascript">
        @Html.Action("Index", "Generated")
</script>*@

<div class="jumbotron">
    <img src="~/Images/aptuitlogo.png" />
    <p class="lead">
        Aptuit's real time client data portal
    </p>
</div>



<div class="container" ng-app="angularTreeview" ng-controller="HomeController">
    <div class="row">
        <div class="col-md-2">
            <div 
                 data-angular-treeview="true"
                 data-tree-id="tree"
                 data-tree-model="browser"
                 data-node-id="id"
                 data-node-label="label"
                 data-node-children="children">
            </div>
        </div>
        <div class="col-md-10">
            <table class="table table-striped table-hover table-condensed">
                <thead>
                    <tr>
                        <th class="col-lg-2">
                        </th>
                        <th class="col-lg-10">
                            Name
                        </th>
                    </tr>
                </thead>

                <tbody ng-cloak>
                    <tr ng-repeat="item in items" >
                        
                        <td class="col-lg-2" ng-switch="item.MimeType">
                            <div class="well well-lg text-center" ng-switch-when="@MimeTypes.PlainText.MimeType">
                                <h1 class="glyphicon glyphicon-file"></h1>
                            </div>
                            <img class="img-thumbnail" ng-cloak ng-switch-default ng-src="{{item.image}}" />
                        </td>

                        <td class="col-lg-9">
                            <h5>{{item.Name}}</h5>
                        </td>

                        <td class="col-lg-1">
                            <a class="btn btn-default" href="/api/content/{{item.Id}}">
                                <span class="glyphicon glyphicon-download"></span> Download
                            </a>
                        </td>
                    </tr>
                </tbody>
            </table>
            <button class="btn btn-primary col-lg-offset-10 col-lg-2"
                    data-toggle="modal"
                    data-target="#uploadModal">
                <span class="glyphicon glyphicon-cloud-upload"></span> Add new file
            </button>
        </div>
    </div>
</div>

<div class="modal fade" id="uploadModal" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class=" modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">Upload file</h4>
            </div>
            <form name="newFile" method="post" enctype="multipart/form-data" action="~/api/upload">
                <div class="modal-body">
                    <p>Select the file to upload</p>
                    <input name="file" type="file" />
                    <input name="path" type="hidden" value="pathpathpath"/>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </form>
        </div>
    </div>
</div>
